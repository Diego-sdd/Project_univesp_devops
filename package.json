{
  "name": "univesp_donation",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server.js",
    "build": "rimraf ./build && tsc",
    "dev": "nodemon src/server.ts"
  },
  "author": "Diego Dias",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
  }
}
